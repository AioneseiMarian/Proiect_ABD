<Window x:Class="Proiect_ABD.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Proiect_ABD.View_Model"
        Title="Login" Height="550" Width="900">
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Primul rând: Username -->
        <Label x:Name="lbl_username" Content="Username" Grid.Row="0" Grid.Column="0" Margin="10" FontSize="20" HorizontalAlignment="Center"/>
        <TextBox x:Name="txtUsername" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="10" Width="306" HorizontalAlignment="Center" FontSize="24"/>

        <!-- Al doilea rând: Password -->
        <Label x:Name="lbl_password" Content="Password" Grid.Row="1" Grid.Column="0" Margin="10" FontSize="20" HorizontalAlignment="Center"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" PasswordChanged="PasswordBox_PasswordChanged" Width="306" FontSize="24" Margin="10" Height="40.04" HorizontalAlignment="Center"/>

        <!-- Al treilea rând: Butoane -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="lbl_login" Content="Login" Command="{Binding LoginCommand}" Width="110" Margin="5"/>
            <Button x:Name="lbl_register" Content="Register" Command="{Binding GoToRegisterCommand}" Width="105" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>